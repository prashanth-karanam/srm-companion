[build]
builder = "nixpacks"

[deploy]
startCommand = "python backend_server.py"
healthcheckPath = "/api/status"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "srm-companion-backend"
