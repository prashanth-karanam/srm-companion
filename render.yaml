services:
  - type: web
    name: srm-companion-wa-bridge
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install
    startCommand: node wa_bridge.js
    envVars:
      - key: PORT
        value: 8001
      - key: NODE_ENV
        value: production
